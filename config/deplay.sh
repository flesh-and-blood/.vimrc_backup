#! /usr/bin/bash

cp -r c.vim "$HOME/.vim/syntax/"
cp -r .gitconfig "$HOME"
cp -r .xprofile "$HOME"
cp -r .bashrc "$HOME"
cp -r .vimrc "$HOME"
cp -r .pam_environment "$HOME"
cp -r .bash_profile "$HOME"
cp -r .profile "$HOME"
cp -r bld "$HOME/Documents/usr/bin"